"""
script for building images for the docs for the colorschemes
"""

from py_gd import Image
from py_gd import colors

for colorscheme in colororscheme_names:
    if colorscheme[1] == `continuous`:
        img = Image(600, 100, preset_colors=colorscheme[0])


